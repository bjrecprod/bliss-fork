# Test database — isolated `bijoyai_test` DB (not production).
# Local dev: PostgreSQL role `bjrectest` with trust auth before the SCRAM rule in pg_hba.conf.
# Loaded by src/__tests__/setup/env.js before root .env (which provides all other vars).
DATABASE_URL="postgresql://bjrectest@localhost:5432/bijoyai_test"
